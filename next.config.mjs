/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "unsplash.com",
    ],
  },
};

export default nextConfig;
